---
name: shape
description: 在方向、任务承诺或承重前提尚不可靠，用户的地图可能与真实项目不一致，或新证据推翻既有方向时使用。接触真实对象，让事实、可能性、代价和用户价值显形，再形成或修订可承诺的 Task Context。方向清楚的低风险任务，以及仅恢复或持久化已有 Context，不触发本 Skill。
---

# shape

## 先让领地说话

用户的请求、计划和当前上下文是一张地图；代码、运行现场、真实用户和现实约束才是领地。不要先把地图整理成答案。接触真实对象，让事实、矛盾、可能性、代价与仍未看清的部分共同显形，再判断问题是什么。

调查到哪里，结论才能代表到哪里。能从领地取得的事实自行取得；只有价值、偏好、代价接受和授权属于用户。给出明确的专业推荐，在证据反对原方向时直接说明，但不替用户作价值决定。

## 让方向取得承诺资格

探索保持开放，直到继续调查已不再实质改变选择，关键代价已经看清，可观察结果、不能破坏的行为和证据要求也能够说明。

此时收敛方向，把实现路径留给后续现实反馈。调查可以超出当前执行范围；发现更大机会或问题不等于获得实施授权。

方向不是永久权威。新证据推翻承重前提时，重新开放受影响的决定；当前上下文本身已经被旧前提污染时，使用 `rewind` 恢复可信状态。

## 形成共享 Task Context

收敛时写清目标、scope、non-goals、完成证据、必须保持的行为，以及本轮真实相关的 Task Operating Envelope。包络必须来自 Project Context 的现实证据，不从理论最坏情况生成。

任务涉及代码或分支时，澄清 `source_ref`、`working_branch` 和 `target_ref` 的意图；现场状态与意图不一致且会改变结果时，把事实和代价交给用户确认，不从 checkout 反推承诺。

简单任务可以在对话中维护等价状态。需要持久 Task Context 时读取 [task-context.md](references/task-context.md)，把权威状态写入 `<task-workspace>/task.md`。

用户可以改变 scope；Agent 也可以主动提出更诚实的扩大、收缩或改道。后者在用户同意前只是候选。任何会改变下游判断的 Task Context 变化增加 `context_revision`；承诺变化同时增加 `scope_revision`，并标出哪些设计、实现、review 或 test 结论需要重新核验。

## 长程探索

探索跨越多个决定或会话、当前上下文不足以保存可靠状态时，读取 [open-territory.md](references/open-territory.md)。

## 产物

只有方向比较、代价或关键未知需要交接时，才在 Task Context 之外保存 Shape 过程：

```text
<task-workspace>/
├── task.md             # 当前任务的权威 Context
└── shape/
    ├── shape.md        # 只有方向比较、代价或关键未知需要交接时创建
    └── evidence/       # 只有结论依赖探针、运行结果或外部材料时创建
```

`task.md` 只保存当前权威状态和证据句柄；调查日志、原始证据、被放弃路线和过程叙事不复制进去。

`shape.md` 只记录真正改变方向的事实、仍有竞争力的路线、推荐及其代价、用户决定和剩余承重未知；可重放探针与原始材料进入 `evidence/`。

## 边界

Shape 只让方向变得值得承诺，不负责详细设计、实现、评审或测试，也不借调查扩大执行范围。
