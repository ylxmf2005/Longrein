---
artifact_type: TestPlan
component: e2e
task_id: 20260603-120000-example-task
author_agent: test-planner
parent: test/test-plan.md
---

# E2E 测试手册：示例标题

## 执行模式

- 默认：浏览器操作为主要证明方式（真实浏览器、已登录会话）；截图和页面状态为主要证据，API/DB/日志为辅助证据。
- 降级：仅在此处明确声明时使用；说明降级后哪些内容无法再被证明。

## 前置条件

- 入口 URL、登录方式（仅引用凭据）、测试数据准备（精确到对象 ID；在此补充上下文文档未涵盖的内容）。

## FLOW-1 (P1): 用户目标名称

- 角色：高级用户。覆盖：FR-x / AC-y。

| # | 页面/位置 | 操作（字面输入） | 期望行为 | 证据 |
|---|---|---|---|---|
| 1 | 任务列表页 `<URL>` | 点击"新建任务" | 创建对话框打开 | 截图 |
| 2 | 创建对话框 | 在提示词输入框中输入：`帮我把 README 翻译成英文` | 提交后跳转到任务详情页 | 截图，记录 task_id |
| 3 | 任务详情页 | 等待执行完成 | 状态变为成功，制品可下载 | 截图；辅助证据 `GET /api/task/<id>` |

- 错误路径（同样逐步编写）：

| # | 页面/位置 | 操作 | 期望行为 | 证据 |
|---|---|---|---|---|
| 1 | 创建对话框 | 提交空提示词 | 内联错误提示，未发送请求 | 截图，网络面板 |

- 阻塞条件：当环境/路由/数据缺少某些内容时，将此流程标记为阻塞并说明缺口。
