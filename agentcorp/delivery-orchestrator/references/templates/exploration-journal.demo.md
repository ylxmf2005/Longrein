# 探索日志

## 第 1 轮（C-1：任务列表页）

1. 打开 `/app/tasks` → 看到任务列表与「新建」按钮 → `shots/01-task-list.png`
2. 点击「新建」 → 弹出创建对话框，含 prompt 输入框与提交按钮 → `shots/02-create-dialog.png`
3. 观察：prompt 为空时提交按钮置灰（已记入踩坑）。

新入口入 frontier：左侧导航有「设置」。
