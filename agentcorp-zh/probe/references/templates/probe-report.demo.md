---
artifact_type: ProbeReport
task_id: 20260603-120000-example-task
author_agent: probe
status: in_progress
source_artifacts:
  - none
---

# Probe 报告 — <主题>

<!--
上面的 frontmatter 是 artifact 契约。下面的章节只是一个初始骨架，不是固定格式：
保留对地形有用的部分，删掉用不上的，缺什么就补什么——但绝不能删掉 Unknowns Ledger、
“你问的 vs 领地实际的样子”和“如何更好地下指令”——最后这一节是 `brainstorm`
和 `validate-requirements` 的 handoff 供给。这是一份活文档：记录解决后原地更新；
已解决的记录保留可见并附证据。sponsor 确认地形已定之前 status 保持
in_progress，之后改为 completed。
-->

## 你问的 vs 领地实际的样子

- 你假设 <sponsor 请求中的假设>；领地显示 <纠正后的事实>（`path/to/file.py:120`）。
- （至多五条纠正。每条都有锚点。这一节决定报告值不值得读。）
- （如果地图确实成立：如实说明，并列出你检查了什么才得出这个结论。）

## 地形

（由宽到窄地教：sponsor 需要的概念、各个活动部件及其各自职责、端到端的流程。
先直觉后机制。每条断言都要有锚点——`file:line`、命令 + 输出摘录、commit、文档路径、
带日期的 URL。区分事实 / 推断 / 假设。）

## 本来会让你吃惊的事

- <历史、坑、看着不对但有意为之的设计、隐藏约束、repo 内先例——附锚点。>
- （如果确实没有：说明这片地形为什么简单，以及你检查了什么才得出这个结论。）

## 这里的“好”长什么样

- <本地惯例、应当模仿的范例文件、改动被期望呈现的形状。>

## Unknowns Ledger

| ID | 未知 | 阻塞 | 已尝试 | 最佳假设 | Owner | 如何解决 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| U-1 | <未知是什么> | <阻塞哪个决策> | <我已经做了什么> | <假设，标注为假设> | probe / sponsor / together | <具体下一步> | open |
| U-2 | <已解决示例> | <决策> | <已尝试> | <假设> | sponsor | <步骤> | resolved: <证据> |

没有就写 "none"。

## 如何更好地下指令

- <三到六条 sponsor 现在能明确指定、probe 之前无法指定的具体事项。>

---

交付前自检——命中任何一条就回去重写：

- 某条关键断言没有锚点（file:line、命令输出、commit、文档、带日期的 URL）。
- 某条台账记录是光秃秃的问题——没有“已尝试”、没有假设、没有 owner。
- “本来会让你吃惊的事”是空的，却没有明确的“确实简单，因为……”辩护。
- 报告只回答了字面请求，没有补上任何 sponsor 不知道要问的东西。
- 第一节里的某条“纠正”是制造出来的——把小挑剔夸大成头条，而不是诚实地说“地图成立”。
- 某个被有意跳过的扫掠来源没有在台账里被点名为未扫掠。
- 某个零上下文读者需要的章节（某术语什么意思、某组件是干什么的）默认了 repo 知识。
- 记录仍处于 open 状态时整份文件被推倒重写，而不是原地更新。
