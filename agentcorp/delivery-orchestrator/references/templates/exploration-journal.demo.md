<!-- Exploration working notes for filling testing-context — deliberately no YAML frontmatter; never named in a receipt, so validate-handoff.py never sees them. -->
# Exploration Journal

## Round 1 (C-1: task list page)

1. Open `/app/tasks` → see the task list and the "New" button → `shots/01-task-list.png`
2. Click "New" → the create dialog opens, with a prompt field and a submit button → `shots/02-create-dialog.png`
3. Observation: the submit button is grayed out when the prompt is empty (recorded as a gotcha).

New entry point added to frontier: there is a "Settings" item in the left nav.
